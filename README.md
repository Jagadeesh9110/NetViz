# NetViz
NetViz is a full-stack networking simulator that shows how a reliable data transfer protocol works internally. I will build your own “mini-TCP” over UDP, then visualize the packet flow in real time using React,Node.js, Java(for netwroking + protocol Logic).
